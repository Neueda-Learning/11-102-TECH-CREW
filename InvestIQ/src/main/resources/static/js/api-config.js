// Frontend runtime config for third-party APIs.
// Note: any key here is visible in the browser.
window.ALPHA_VANTAGE_SEARCH_API_KEY = "92DHN2Q250CQCTB0";

// Optional backend base URL override used by dashboard.js.
// Keep empty to auto-detect same-origin, then fallback localhost ports.
window.INVESTIQ_API_BASE_URL = "http://localhost:8083";

// USD to INR conversion endpoint used by dashboard.js.
window.USD_INR_API_URL = "https://open.er-api.com/v6/latest/USD";

